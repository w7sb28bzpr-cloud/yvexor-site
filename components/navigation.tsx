"use client";
import{CSSProperties,useEffect,useState}from"react";import{BrandLogo}from"./brand-logo";import{Icon}from"./ui";
const items=[{label:"Accueil",href:"#accueil",icon:"home"},{label:"Solutions",href:"#solutions",icon:"solutions"},{label:"Projet",href:"#assistant",icon:"project",featured:true},{label:"Exemples",href:"#exemples",icon:"sectors"},{label:"YVEXOR",href:"#yvexor",icon:"brand"}];
export function SiteHeader(){const[scrolled,setScrolled]=useState(false);useEffect(()=>{const onScroll=()=>setScrolled(scrollY>12);onScroll();addEventListener("scroll",onScroll,{passive:true});return()=>removeEventListener("scroll",onScroll)},[]);return <header className={`site-header ${scrolled?"is-scrolled":""}`}><a href="#accueil" aria-label="YVEXOR — Accueil"><BrandLogo/></a><nav aria-label="Navigation principale"><a href="#solutions">Solutions</a><a href="#caisse-commerce">Caisse & Commerce</a><a href="#budgets">Budgets</a><a href="#exemples">Exemples</a><a href="#yvexor">YVEXOR</a></nav><a className="header-cta" href="#assistant">Parler de mon projet</a></header>}
export function MobileTabBar(){const[active,setActive]=useState("#accueil");useEffect(()=>{const ids=items.map(i=>i.href.slice(1)),observer=new IntersectionObserver(es=>{const visible=es.filter(e=>e.isIntersecting).sort((a,b)=>b.intersectionRatio-a.intersectionRatio)[0];if(visible)setActive(`#${visible.target.id}`)},{rootMargin:"-30% 0px -55%",threshold:[0,.2,.5]});ids.forEach(id=>{const el=document.getElementById(id);if(el)observer.observe(el)});return()=>observer.disconnect()},[]);return <nav className="mobile-tabs" aria-label="Navigation mobile"><i className="mobile-active-light" style={{"--active-index":items.findIndex(item=>item.href===active)} as CSSProperties}/>{items.map(item=><a key={item.href} href={item.href} className={`${active===item.href?"is-active":""} ${item.featured?"is-featured":""}`} aria-current={active===item.href?"page":undefined} onClick={()=>setActive(item.href)}><span><Icon name={item.icon}/></span><small>{item.label}</small></a>)}</nav>}
