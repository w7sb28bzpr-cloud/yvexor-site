# Bootstrap: no application or sign-in is served over plain HTTP.
server {
    listen 80;
    listen [::]:80;
    server_name client.yvexor.com admin.yvexor.com;
    access_log off;
    location /.well-known/acme-challenge/ { root /var/www/yvexor-acme; }
    location / { return 503; }
}
