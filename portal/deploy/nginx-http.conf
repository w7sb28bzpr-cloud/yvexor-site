# HTTP bootstrap only. Do not expose sign-in until certificates are installed.
server {
    listen 80;
    listen [::]:80;
    server_name client.yvexor.com admin.yvexor.com;
    location /.well-known/acme-challenge/ { root /var/www/yvexor-acme; }
    location / { return 503; }
}
